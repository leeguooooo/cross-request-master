{
  "name": "yapi-plugin",
  "version": "0.1.2",
  "private": true,
  "description": "Cursor and Claude Code plugin for YApi CLI workflows.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-template.mjs"
  }
}
